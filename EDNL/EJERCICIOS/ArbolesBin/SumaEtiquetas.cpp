#include "..\..\Implementaciones\Abin.hpp"

/**
 * @brief Implemente una función llamada sumaEtiquetas que dados dos árboles
 * binarios de tipo entero, devuelva la suma total de los nodos de las ramas
 * (de raíz a hoja) de ambos árboles si se cumple que las mismas ramas de
 * cada árbol suman igual. Se devolverá 0 en caso contrario.
 */
int sumaEtiquetas(Abin<int>& a, Abin<int>& b) {}